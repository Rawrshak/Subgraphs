type ContractRegistry @entity {
  id: ID!
  contentManagers: [ContentManager!] @derivedFrom(field: "registry")
}

type ContentManager @entity {
  id: ID!
  content: Content!
  creator: Account!
  registry: ContractRegistry!
}

type Asset @entity{
  id: ID!
  tokenId: BigInt!
  parentContract: Content!
  currentSupply: BigInt!
  maxSupply: BigInt!
  balances: [AssetBalance!] @derivedFrom(field: "asset")
  assetRoyalties: [AssetFee!]!
  latestHiddenUriVersion: BigInt!
  latestPublicUriVersion: BigInt!
  transactions: [Transaction!]! @derivedFrom(field: "asset")
  mintCount: BigInt!
  burnCount: BigInt!
}

type AssetBalance @entity {
  id: ID!
  asset: Asset!
  owner: Account!
  amount: BigInt!
}

type AssetFee @entity {
  id: ID!
  asset: Asset!
  creator: Account!
  rate: BigInt!
}

type ContractFee @entity {
  id: ID!
  content: Content!
  creator: Account!
  rate: BigInt!
}

type Content @entity {
  id: ID!
  contractAddress: Bytes!
  contractUri: String!
  assets: [Asset!]! @derivedFrom(field: "parentContract")
  contractRoyalties: [ContractFee!]!
  approvals: [Approval!]! @derivedFrom(field: "content")
  minters: [Account!]
}

type Account @entity {
  id: ID!
  address: Bytes!
  assetBalances: [AssetBalance!]! @derivedFrom(field: "owner")
  approvals: [Approval!]! @derivedFrom(field: "user")
  transactions: [Transaction!]!
  mintCount: BigInt!
  burnCount: BigInt!
  transactionOperator: [Transaction!]! @derivedFrom(field: "operator")
}

type Approval @entity {
  id: ID!
  content: Content!
  user: Account!
  operators: [Account!]!
}

enum TransactionType {
  Mint
  Burn
}

type Transaction @entity {
  id: ID!
  operator: Account!
  user: Account!
  transactionType: TransactionType!
  asset: Asset! 
  amount: BigInt!
  blockNumber: BigInt!
  timestamp: BigInt!
  gasUSed: BigInt!
  gasPrice: BigInt!
}